import XCTest
@testable import network_combine_stack

final class network_combine_stackTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(network_combine_stack().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
